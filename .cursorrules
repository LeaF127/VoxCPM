## 项目背景
- 本仓库是 **VoxCPM** 文本转语音（TTS）与语音克隆项目，核心代码位于 `src/voxcpm`。
- 主要语言为 **Python 3.10+**，依赖 `torch`, `torchaudio`, `transformers`, `gradio` 等。
- 已配置 `black`，行宽为 **120**。

## 核心原则
- **全局视角**：利用Cursor的上下文感知能力，理解整个项目架构，确保全局正确性和一致性。
- **用户视角**：始终站在用户角度思考问题，理解用户需求，提供最符合用户期望的解决方案。比如安装环境和依赖，不是给我本地环境安装了解决了就可以了，核心是用户那边遇到这些问题的处理流程，有一些方案如果依赖到用户手动再安装的话，就不要用了
- **主动沟通**：遇到不确定的地方必须征求我的意见，不要擅自决策；我说错的地方要直接指出，千万不要盲目认同

## 通用协作规则
- **默认语言**
  - 所有解释、文档性文字与示例说明，默认使用 **简体中文**。
  - 若上游仓库内容为英文，保留原文注释和文档，仅在需要时补充中文说明。

- **代码风格与质量**
  - Python 代码严格遵守 `pyproject.toml` 中的 `black` 配置（行宽 120 等）。
  - 新增或修改代码时，尽量保持原有风格（函数命名、模块结构、类型/注释风格）。
  - 涉及公共接口（如 `voxcpm.VoxCPM`、`voxcpm.cli`）时，避免随意破坏现有 API 行为；如需变更，优先兼容扩展或在文档中明确说明。

- **测试与运行**
  - 优先通过 `pytest` 进行测试（若需要编写新测试，存放在项目根或 `tests/` / 相关模块附近）。
  - 测试与运行由我来执行，并给出测试结果，你只需要给出测试代码和运行命令，不要自己执行测试

## 目录与文件级约束
- **核心代码**
  - 修改 TTS / 推理主逻辑时，优先在以下目录中寻找与修改：
    - `src/voxcpm/model`：模型封装与加载逻辑。
    - `src/voxcpm/modules`：各子模块（`audiovae`, `locdit`, `locenc`, `minicpm4` 等）。
    - `src/voxcpm/stream`：流式推理管线。
    - `src/voxcpm/utils`：工具函数（文本正则化、音频处理等）。
  - 变更推理接口时，应同时考虑：
    - Python API：`voxcpm.VoxCPM` 类。
    - CLI 接口：`voxcpm.cli` 与 `voxcpm/cli/stream_translate.py`。

- **演示与 Web 界面**
  - 与 Web Demo / Gradio 相关的改动集中在：
    - `app.py`
    - `api_server.py`
    - `api/` 目录
  - 修改 Web UI 时，保证现有功能（普通 TTS、语音克隆、流式输出）不被破坏，除非用户明确要求。

- **数据与资源**
  - 不修改 `assets/` 中的图片及静态资源内容（除非为前端展示主动更新）。
  - 不在仓库内提交体积较大的新模型权重或音频数据，优先使用 Hugging Face 或 ModelScope 下载。

## 工具与自动化使用建议
- **代码理解与导航**
  - 需要了解项目结构、数据流或模块职责时，可使用：
    - `codebase_search`：按语义搜索模型加载、推理、流式输出等逻辑。
    - `grep`：按精确字符串（如函数/类名、关键配置）进行搜索。

- **编辑与重构**
  - 对现有文件进行改动时，使用 `apply_patch` 工具生成最小化 diff，保持变更聚焦、可读。
  - 对 Notebook（若未来新增）使用 `edit_notebook` 工具进行修改。

- **检查与修复**
  - 对改动过的 Python 文件，使用 `read_lints` 检查 linter 问题，仅在易于修复时调整代码。
  - 不强行修复大量历史/无关 lints，除非它们直接影响当前修改的功能。

## 风格与文档
- **注释与文档**
  - 新增复杂逻辑时，优先增加简洁的中文注释，必要时附英文补充。
  - 若修改会影响 README 中描述的调用方式（API/CLI/参数），应同步更新 `README.md` 中相关段落（或在答复中提醒用户）。

- **示例代码**
  - 在回答用户问题时，尽量给出贴近本项目的示例（使用 `VoxCPM.from_pretrained`、`generate`、`generate_streaming`、CLI `voxcpm` 等）。
  - 非本仓库的通用示例需明确标注为“示例/伪代码”，避免与真实项目实现混淆。

## 安全与合规
- 任何涉及 **语音克隆 / 声纹模仿 / 深度伪造** 的功能说明中，应提醒用户遵守法律与伦理规范，不得用于违法或不当用途。
- 不擅自加入会自动上传用户音频/文本到第三方的逻辑；如确需远程调用服务，必须在说明中显式告知。

## 回答风格
- 回答用户时：
  - 使用 **简体中文**，结构清晰，适当使用小节标题（`##` / `###`）与列表。
  - 对于简单问题，简明扼要；对于涉及代码改动的问题，说明修改位置与影响范围。
  - 总结性说明中，避免粘贴过长的代码块，仅在必要时给出关键片段。


